package com.loop.test.day9_driver_faker_action;

import com.loop.test.utilities.ConfigurationReader;
import com.loop.test.utilities.Driver;
import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

import org.openqa.selenium.interactions.Actions;
import org.testng.annotations.Test;

import javax.swing.*;

public class T4_hoverover {

    /*
    1. Go to https://www.google.com/
    2. Hover over on search button
    3. Hover over on Feeling Lucky button

     */

    @Test

    public  void  google_actions_hover() throws InterruptedException {
// Driver.getDriver().get(ConfigurationReader.getProperty("google.url"));
//        WebElement googleSearchBox = Driver.getDriver().findElement(By.name("btnK"));
//        WebElement fillingLucky = Driver.getDriver().findElement(By.xpath("(//input[@name='btnI'])[1]"));
//        Actions action= new Actions(Driver.getDriver());
//        Thread.sleep(3000);
//        action.moveToElement(googleSearchBox).perform();
//        Thread.sleep(5000);
//        action.moveToElement(fillingLucky).perform();



    }
}
